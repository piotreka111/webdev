<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" />
    <title>Tytul</title>
</head>
<body>
    <div class="container mt-2">
        <h1>Tagi</h1>
        <% %>if deleteResult}}
            {{this.message}}
        {{/if}}
        {{#each tags}}
            <p>
                {{this.description}} <a href="/tags/{{this._id}}">link</a>
            <form action="/tags?delete={{this._id}}" method="POST">
                <input type="submit" value="delete" />
            </form>
            </p>
        {{/each}}
    </div>
</body>
</html>